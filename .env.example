# Backend
MONGO_URI=your_mongo_connection_string_here
PORT=4000
JWT_SECRET=your_jwt_secret_here
NODE_ENV=development

# Frontend
# If frontend uses API_BASE_URL in src/config.ts or environment, set it when deploying
API_BASE_URL=http://localhost:4000

# Notes: Do NOT commit real secrets to the repository. Use your deployment provider's secret store for production.
